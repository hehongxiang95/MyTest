<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".layout.LinearActivity" />
        <activity android:name=".layout.LayoutActivity" />
        <activity android:name=".layout.RelativeActivity" />
        <activity android:name=".layout.FrameActivity" />
        <activity android:name=".scroll.ScrollActivity" />
        <activity android:name=".viewgroup.OverFrameLayoutActivity" />
        <activity android:name=".flow.FlowActivity" />
        <activity android:name=".waterfall.WaterFallActivity" />
        <activity android:name=".recycler.RecyclerActivity" />
        <activity android:name=".viewgroup.OverRelativeActivity" />
        <activity android:name=".layout.GridLayoutActivity"></activity>
    </application>

</manifest>